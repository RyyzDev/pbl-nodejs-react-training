npm install memungkinkan kita untuk menginstall dependensi yang telah di list di package.json
sementara npm install <nama library>, memungkinkan kita untuk menginstall dependensi baru lalu di catat di package.json

