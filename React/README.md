npm install memungkinkan kita untuk menginstall dependensi yang telah di list di package.json
sementara npm install <nama library>, memungkinkan kita untuk menginstall dependensi baru lalu di catat di package.json

yang membaca script yang kita buat yang pertama adalah developernya, manusia, dan sistem.,

state adalah nilai awal, { const [posts, setPost] = useState([])}
posts adalah nama wadah yang akan menampung data, sementara setPost adalah nama fungsi untuk melakukan perubahan wadah tersebut
useState(value) adalah nilai awal dari wadah tersebut.